import React from 'react';
import ResourceMonitor from '../../system-monitor/resources';

const SystemResourceMonitor: React.FC = () => {
  return <ResourceMonitor />;
};

export default SystemResourceMonitor;