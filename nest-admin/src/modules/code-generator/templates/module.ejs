import { Module } from '@nestjs/common';
import { <%= className %>Controller } from './controllers/<%= helper.toKebabCase(className) %>.controller';
import { <%= className %>Service } from './services/<%= helper.toKebabCase(className) %>.service';
import { PrismaModule } from '@/shared/prisma/prisma.module';

/**
 * <%= generator.businessName %> 模块
 * @author <%= generator.functionAuthor %>
 * @date <%= new Date().toISOString().slice(0, 10) %>
 */
@Module({
  imports: [PrismaModule],
  controllers: [<%= className %>Controller],
  providers: [<%= className %>Service],
  exports: [<%= className %>Service],
})
export class <%= helper.toPascalCase(generator.businessName) %>Module {}